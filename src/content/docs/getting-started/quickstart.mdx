---
title: Quickstart
description: Get your farm running in under 5 minutes.
---

Everything you need to go from zero to running AI containers on your own hardware.

## 1. Install the CLI

```bash
npm install -g @crops-code/cli
cropscode --version
```

Requires Node.js 22+. See [Install CLI](/getting-started/install-cli) for details.

## 2. Log in

```bash
cropscode login
```

This opens your browser for a secure OAuth sign-in with your CropsCode account.

## 3. Register & start a worker

```bash
# Register this machine (one-time setup)
cropscode worker --register

# Start the worker
cropscode worker
```

The worker connects outbound to the orchestrator — no inbound ports, no firewall rules.

Or run the guided wizard that handles everything:

```bash
cropscode onboard
```

## 4. Launch your first container

1. Open the dashboard at your orchestrator URL
2. Paste a GitHub issue link into the launch modal
3. Pick a worker and click **Launch**

Within 30 seconds, a container is running and the web terminal is live. Your AI agent is now working on the ticket.

## What's next?

- [Launch from a ticket](/guides/launch-from-ticket) — full walkthrough of the launch flow
- [Workers concept](/concepts/workers) — understand self-hosted vs managed
- [CLI reference](/cli/overview) — all available commands
